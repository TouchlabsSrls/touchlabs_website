# Video sorgente (non deploy)

File master ad alta risoluzione **non serviti** dal sito pubblico (`public/`).

| File | Uso |
|------|-----|
| `touchlabs-showreel-original.mp4` | Master showreel (~72 MB). Il deploy usa `public/assets/video/touchlabs-showreel.webm` e `touchlabs-showreel-compressed.mp4`. |

In produzione copiare solo `public/` sul server — questa cartella resta fuori dal deploy.
