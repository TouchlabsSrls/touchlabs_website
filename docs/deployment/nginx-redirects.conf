# Redirect 301 — URL legacy Touchlabs
# Includere nel server block nginx del sito (es. /etc/nginx/conf.d/touchlabs.conf)

# Digital Experience (ex Configuratori 3D)
location = /configuratori-3d.html {
    return 301 http://$http_host/digital-experience.html;
}

# Opzionale: normalizzazione trailing slash home
# location = /index.html {
#     return 301 /;
# }

# PHP API — form contatti (richiede fastcgi_pass verso PHP-FPM)
# location ~ ^/api/contact\.php$ {
#     include fastcgi_params;
#     fastcgi_param SCRIPT_FILENAME /var/www/api/contact.php;
#     fastcgi_pass php:9000;
# }
